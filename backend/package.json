{
  "name": "backend",
  "version": "1.0.0",
  "description": " Build a web application that allows a user to upload an ad data. The application will then analyze the ad data and provide a summary of the ad's performance based on the analysis and agent will create one or more tasks based on the outcome (e.g. update keywords).",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.1.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "langchain": "^0.3.13",
    "multer": "^1.4.5-lts.1"
  }
}
